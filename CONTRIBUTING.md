# How to contribute

Thank you for considering contributing to this plugin!

## How to build the plugin

1. Fork this repository with your GitHub account
2. Clone your fork
3. Install `pnpm` if you haven't
4. Install dependencies with `pnpm i`
5. Use `pnpm dev` to build the plugin in dev mode, `pnpm build` to build the plugin in prod mode
